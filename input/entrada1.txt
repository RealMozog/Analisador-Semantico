const {
    int a = 4;
    boolean aux1 = true;
    real numeroAux = 4.478;
    string auxString = "Testando";
} 
var {
    int auxNum[2][10], testeNum, next;
    string auxString;
	struct istruct extends j {
	var {
	int varia;
}
	struct inteiro {
	var {
	int varia;
}
}

function int starti (){
    var {
        int numLocal[5];
        real numAux;
    }
    numLocal = 3;
    numAux = 1.1;
    while (numLocal < 3) {
        a = mudaNum (a);
    }

    aumentaNum(a);
    return 0;
}

function inteiro mudaNum (real a, int b){
    var {}    
    if ((a == c && c <= d)) then {
            a++;
        } else {
            if (a < 5) then {
                b = a.besta * 3;
            } else {
                    if (a < 10) then {
                    a = a + 3;
                    }
            }
        }
        return a;
}

procedure addNum(real a, int c){
    var {}   
    a = a + 20;
}
