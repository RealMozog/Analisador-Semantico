const {
	int a = 10, d = 20;
	real b = 22.2;
	string c = "teste tipo";
}

var { }

function real somar (real x, real y) {
	var { real result; }
		
	local.result = local.x + local.y + global.a;  // erro01: tipos incompativeis
			
	return local.result;
}	

function string imprimir (string msg) {
	var { int b; }
	
	global.b = local.msg;  	 // erro02: tipos incompativeis 
		
	return global.b;    // erro03: tipos incompativeis 
}

procedure start ( ) { 
	var { }	
}
